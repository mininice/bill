/**
 * 创建客栈
 */
export default async function (params) {

  try {
    return {
    	todoList: [
    		{
    			name: 'ksjdf',
    			pid: 123	
    		}
    	]
    };
  } catch (e) {
    // nothing to do (now)
  }

  return null;
}
